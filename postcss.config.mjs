/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    // https://github.com/postcss/autoprefixer
    autoprefixer: {},
  },
};

export default config;
